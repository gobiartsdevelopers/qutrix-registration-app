{
  "devDependencies": {
    "turbo": "^1.10.12"
  },
  "workspaces":[
    "apps/*"
  ],
  "scripts": {
    "dev":"turbo run dev",
    "build":"turbo run build",
    "api:prod":"node apps/api/dist/main"
  }
}
